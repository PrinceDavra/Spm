import { StudentTranscriptView } from "@/components/exam/student-transcript-view";

export const metadata = {
  title: "Official Academic Transcript | CampusSphere",
  description: "View and download official institutional academic transcript and cumulative evaluation history.",
};

export default function StudentTranscriptPage() {
  return <StudentTranscriptView />;
}
